import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    backButton: {
        width: 38,
        height: 38,
        borderRadius: 19, // Hace el contenedor completamente circular
        alignItems: "center",
        justifyContent: "center",
        marginLeft: 4, // Ajuste para alineación con el margen izquierdo nativo
    },
});
