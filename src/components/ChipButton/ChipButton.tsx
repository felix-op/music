import { View } from "react-native";
import { styles } from "./estilos";

type TProps = {
    // Aquí van las propiedades del componente
};

export function ChipButton(props: TProps) {
    return (
        <View style={styles.container}>
            ChipButton
        </View>
    );
}
